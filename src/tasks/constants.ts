import path from "path";

export const PATHS = {
    input: path.join("src", "data", "input.csv"),
    outputFile: path.resolve("src", "data", "output-file.txt"),
    outputDB: path.resolve("src", "data", "output-db.txt")
};
